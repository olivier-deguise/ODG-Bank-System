
spring:
  datasource:
    platform: postgresql
    username: 'odg18'
    password: 'password'
    url: jdbc:postgresql://postgres-bank:5432/bank

  jpa:
    defer-datasource-initialization: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      #ddl-auto: update
      ddl-auto: create-drop
    show-sql: 'true'

